version: 2

jobs:
  build:
    macos:
      xcode: "9.0"
    steps:
      - checkout
      - working_directory: Example
      - run:
          name: Run tests
          command: fastlane scan
          environment:
            SCAN_DEVICE: iPhone 8
            SCAN_SCHEME: WhatsNew-Example
